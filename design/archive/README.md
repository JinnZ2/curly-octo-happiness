# design/archive

Historical versions of the unified playground (v1-v6; there was never a v4)
and the plugin managers (v1-v3), kept for reading. The current, canonical
files live at the repository root: `unified_playground.py` (successor of v5)
and `plugin_manager.py` (v2 + v3 merged). Do not run or extend these copies.
